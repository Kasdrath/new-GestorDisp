version: '3.8'

services:
  db:
    image: postgres:15
    container_name: gestor_postgres
    environment:
      POSTGRES_USER: gestor_user
      POSTGRES_PASSWORD: gestor_password
      POSTGRES_DB: gestordb
    ports:
      - "5433:5432"
    volumes:
      - gestor_pg_data:/var/lib/postgresql/data

volumes:
  gestor_pg_data:
